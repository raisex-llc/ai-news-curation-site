---
// layouts/Layout.astro
const { currentPage, totalPages, sort } = Astro.props;
const q = Astro.url?.searchParams.get('q') || '';
---

<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>最新AIニュースまとめ</title>
    <script src="/ai-news-curation-site/search.js" defer></script>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <!-- ✅ 固定ヘッダー -->
    <header id="top" class="sticky top-0 z-50 bg-white border-b border-gray-200 px-4 py-3">
      <div class="max-w-screen-lg mx-auto flex flex-col md:flex-row md:items-center md:justify-between gap-2">
        <!-- 左：ロゴ＆タイトル -->
        <a href="/ai-news-curation-site/" class="flex items-center space-x-3 hover:opacity-80">
          <img src="/ai-news-curation-site/assets/ai-icon.png" alt="AIアイコン" class="w-12 h-12" />
          <div class="text-sky-500 text-2xl font-bold">最新AIニュースまとめ</div>
        </a>

        <!-- 右：（ページ1 / 全XXページ中） -->
        {currentPage && totalPages && (
          <div class="text-sm text-gray-600 font-normal md:text-right">
            （ページ {currentPage} / 全{totalPages}ページ中）
          </div>
        )}
      </div>

      <!-- 中段：検索フォーム2列 -->
      <div class="mt-3 flex flex-col sm:flex-row justify-center items-center gap-4 px-2 w-full">
        <!-- 左：媒体名プルダウン -->
        <form action="" method="get" class="flex gap-2 w-full sm:w-auto justify-center">
          <select
            name="media"
            class="text-sm border rounded px-2 py-1 w-full sm:w-60"
          >
            <option value="">媒体名で検索</option>
            <option value="arXiv AI">arXiv AI</option>
            <option value="Hugging Face Blog">Hugging Face Blog</option>
            <option value="OpenAI Blog">OpenAI Blog</option>
            <option value="Google AI Blog">Google AI Blog</option>
            <option value="Microsoft Research Blog">Microsoft Research Blog</option>
            <option value="DeepMind Blog">DeepMind Blog</option>
            <option value="MIT">MIT</option>
            <option value="ITmedia AI">ITmedia AI</option>
            <option value="AI Smily">AI Smily</option>
            <option value="AI Shift">AI Shift</option>
          </select>
          <button
            type="submit"
            class="bg-blue-500 text-white text-sm px-3 py-1 rounded hover:bg-blue-600"
          >
            検索
          </button>
        </form>

        <!-- 右：フリーワード検索 -->
        <form action="" method="get" class="flex gap-2 w-full sm:w-auto justify-center">
          <input
            type="text"
            name="q"
            value={q}
            placeholder="記事検索"
            class="text-sm border rounded px-2 py-1 w-full sm:w-72"
          />
          <button
            type="submit"
            class="bg-blue-500 text-white text-sm px-3 py-1 rounded hover:bg-blue-600"
          >
            検索
          </button>
        </form>
      </div>

      <!-- 下段：ページナビゲーション -->
      <div class="mt-2 text-sm flex justify-center gap-4 text-blue-600">
        {currentPage && currentPage > 1 && (
          <a href={`/ai-news-curation-site/page/${sort}/${currentPage - 1}`} class="hover:underline">
            ← 前のページ
          </a>
        )}
        {currentPage && totalPages && currentPage < totalPages && (
          <a href={`/ai-news-curation-site/page/${sort}/${currentPage + 1}`} class="hover:underline">
            次のページ →
          </a>
        )}
      </div>

      <!-- ✅ ヘッダー内広告（max-w-screen-lgで中央寄せ） -->
      <div class="mt-3 w-full flex justify-center">
        <div class="bg-gray-200 text-center py-2 text-sm w-full max-w-screen-lg rounded">
          🔳 ヘッダー内広告募集中
        </div>
      </div>
    </header>

    <!-- ✅ 本文と右カラム -->
    <main class="max-w-6xl mx-auto px-4 py-8 pr-52 flex flex-col gap-6">
      <div class="flex-1">
        <slot />

        <!-- モバイル広告 -->
        <div class="mt-6 space-y-4 block lg:hidden">
          <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
          <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
          <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
        </div>

        <!-- ✅ フッター広告（max-w-screen-lgで中央寄せ） -->
        <div class="mt-8 w-full flex justify-center">
          <div class="bg-gray-200 text-center py-2 text-sm w-full max-w-screen-lg rounded">
            🔳 フッター固定広告募集中
          </div>
        </div>
      </div>
    </main>

    <!-- ✅ 固定右広告（中央固定） -->
    <aside class="hidden lg:block fixed top-48 inset-x-0 mx-auto w-48 z-30 space-y-4">
      <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
      <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
      <div class="bg-gray-300 h-32 flex items-center justify-center">🔳 バナー広告募集中</div>
    </aside>

    <!-- ✅ フッター -->
    <footer class="text-center text-xs text-gray-500 py-6">
      <div class="mb-2 space-x-4">
        {currentPage && currentPage > 1 && (
          <a href={`/ai-news-curation-site/page/${sort}/${currentPage - 1}`} class="text-blue-500">
            ← 前のページ
          </a>
        )}
        {currentPage && currentPage < totalPages && (
          <a href={`/ai-news-curation-site/page/${sort}/${currentPage + 1}`} class="text-blue-500">
            次のページ →
          </a>
        )}
        {currentPage && sort ? (
          <a href={`/ai-news-curation-site/page/${sort}/${currentPage}#top`} class="text-blue-500">
            ページTOPへ
          </a>
        ) : (
          <a href="/ai-news-curation-site/#top" class="text-blue-500">ページTOPへ</a>
        )}
      </div>
      <div>© 2024 raisex, LLC. All rights reserved.</div>
    </footer>
  </body>
</html>
